<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gra.gra">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LogComplaintActivity" />
        <activity android:name=".MyGrievanceActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".MyProfileRequest" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".govtOffcHomeActivity" />
        <activity android:name=".GOGrievanceActivity" />
        <activity android:name=".GOBlockUserActivity" />
        <activity android:name=".GOEmergencyActivity"></activity>
    </application>

</manifest>